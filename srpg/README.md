# SRPG Roguelike

A C-based roguelike SRPG using raylib.

## Project Structure
- `src/`: Source files
- `include/`: Header files
- `assets/`: Sprites, sounds, data files
- `docs/`: Design documents
- `build/`: Build artifacts

## Building
Use `make` to build the project.
